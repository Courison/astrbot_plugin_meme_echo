# astrbot_plugin_meme_echo

当用户发送表情包时，按概率复读相同表情包。

## 配置
- `enable`: 是否启用插件，默认 `true`
- `reread_probability`: 复读概率，0.0 ~ 1.0，默认 `0.3`

## 安装
在 AstrBot WebUI 插件管理中输入本仓库地址安装。

## 更新
在插件管理中点击更新，或手动 `git pull`。